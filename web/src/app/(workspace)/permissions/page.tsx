import { PermissionsScreen } from "./permissions-screen";

export const metadata = { title: "Privacy & permissions — PulseLoop" };

export default function PermissionsPage() {
  return <PermissionsScreen />;
}
