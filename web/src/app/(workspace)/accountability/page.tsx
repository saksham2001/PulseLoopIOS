import { AccountabilityScreen } from "./accountability-screen";

export const metadata = { title: "Accountability — PulseLoop" };

export default function AccountabilityPage() {
  return <AccountabilityScreen />;
}
