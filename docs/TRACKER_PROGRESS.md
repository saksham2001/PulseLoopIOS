# Tracker → Comprehensive AI-First Food/Med/Peptide — Progress Tracker

> Live status for the roadmap in `docs/TRACKER_LOOP_PROMPT.md` §3. One iteration per session. Status: `pending` / `in-progress` / `done`.

## Phase A — Persistence + version foundation
- **A1** `done` — Added `CustomProductInfo` `@Model` (mirrors `SupplementInfo` + `category`/`source`/`isAIGenerated`/`citations`/`createdAt`, all defaulted for lightweight migration) in `Models/LifeOSModels.swift`; registered in `ModelContainerFactory.coreModels` and added to `ProtocolSubApp.models`. Build green, no behavior change. **Next: A2.**
- **A2** `done` — Added `Services/CustomProductStore.swift` (`@MainActor` façade): `upsert` (de-dupe by normalized name+aliases, merges aliases, refreshes fields), `find`/`fuzzyMatch`/`all` over `CustomProductInfo`, and `toSupplementInfo`/`SupplementInfo` convenience upsert. Wired `ProductSearchService.search(query:customMatches:)` to consult persisted custom matches (source `.custom`, conf 0.85) right after the bundled KB and before any network call. Build green. **Next: A3.**
- **A3** `done` — Bumped `ProtocolSubApp` + `NutritionSubApp` `version` to `1.1.0` (explicit `var version`). `CustomProductInfo` is additive (lightweight migration) so no data migration / no `updateNeedsConfirmation` override needed. Added 2 tests to `ModuleVersioningTests` (report 1.1.0; older install surfaces 1.1.0 update). All 12 versioning tests pass. **Phase A complete. Next: B1.**

## Phase B — Unified search engine (Perplexity core)
- **B1** `done` — Added `Services/ProductResearchService.swift` (the AI "Perplexity" tier): `research(query:)` calls `AIService.complete` with a structured-JSON system prompt + OpenRouter `:online` web grounding (retries once without grounding on failure), decodes a typed `ResearchedProduct` with citations/confidence; tolerant JSON extraction, category normalization, SF-Symbol sanitization (rejects emoji), confidence clamp. Extended `ProductSearchResult` with `citations` + `isAIGenerated` and added `ProductSource.aiResearch`; wired research as the final search tier before the deterministic last-resort. Updated `ProductScanView` switch. 6 new parse tests pass; full build compiles. **Next: B2.**
- **B2** `done` — Persist-on-discovery: `ProductSearchService.searchAndPersist(query:in:)` (`@MainActor`) seeds the engine with persisted custom matches, then saves AI-researched results (and uncatalogued FDA/Open Food Facts hits at conf ≥ 0.3) as `CustomProductInfo` via `CustomProductStore` (de-duped), returning `PersistedSearch { results, persistedNames }`. Added `import SwiftData`. 3 store/de-dupe tests pass; build green. **Next: B3.**
- **B3** `done` — Rewired `TrackerView.aiSearchIngredient` to use `ProductSearchService.searchAndPersist(query:in:)` (unified tiered engine + persistence) instead of the bespoke JSON prompt; best result maps into `AISupplementProfile` so the existing result card + `applyAIResult` work unchanged. Added persisted `CustomProductStore` matches to `filteredNameSuggestions` autocomplete. Build green. **Phase B complete. Next: C1.**

## Phase C — Search UI (AI-first surface)
- **C1** `done` — New `Views/ProductSearchView.swift`: AI-first search surface (search field + loading/prompt/empty states, `ProductResultCard` with source badge, benefit/mechanism/dose/timing, warnings, citations, AI disclaimer, "Add to protocol"). Wired into `TrackerView` Protocol header ("Search" button + `showProductSearch` sheet); `adoptSearchResult` inserts a `Medication` from a result. Design-system styled + a11y labels. Build green. **Next: C2.**
- **C2** `done` — New `Services/MealEstimator.swift` (`MealEstimate` + `@MainActor MealEstimator`): AI-first NL meal nutrition estimate (structured JSON, SF-Symbol sanitize, brace-matched JSON extractor) with deterministic `quickEstimate` fallback (no key / offline / parse fail). Rewired `TrackerView.addMealSheet` to show live AI/quick estimate (loading + "Estimate with AI" button), and `saveNewMeal` persists the estimate's macros + note + icon. Build green. **Next: C3.**
- **C3** `done` — `ProductScanView` now routes label scan + manual search through the unified persistence engine: `searchOnline()` and the OCR broad-fallback in `matchProduct(from:)` call `ProductSearchService.searchAndPersist(...)`; `addToProtocol` persists the chosen product to `CustomProductStore` and uses `saveOrLog`. Removed nested `MainActor.run`/`Task` (handler already main-isolated). Build green. **Phase C complete. Next: D1.**

## Phase D — Coach reach
- **D1** `done` — Added `search_product` Coach tool in `ProtocolTools` (registered in `writeTools` since it persists): runs `ProductSearchService.searchAndPersist` and returns up to 5 structured results (name/category/dose/timing/benefit/mechanism/warnings/source/confidence/citations/ai_generated) + `persisted_to_catalog`. Taught via a thorough `description` (use before `create_or_update_medication` for unknown items); prompt builder doesn't hard-code tool names so no extra teaching needed. Build green. **Next: D2.**
- **D2** `done` — Enhanced existing `log_meal` Coach tool (`DailyLifeTools`, already in `writeTools`): when `calories` is omitted/≤0 it auto-fills nutrition via `MealEstimator.estimate(name)` (macros + note + icon), returns `estimated` flag. Updated description to say a natural meal description works and calories are estimated when omitted. Build green. **Phase D complete. Next: E1.**

## Phase E — Production hardening + polish
- **E1** `done` — Robustness audit of the engine surfaces: confirmed `OpenFDAService`/`OpenFoodFactsService`/`ProductSearchService`/`ProductResearchService`/`MealEstimator` have no force-unwraps (all use `guard let`/`try?`/`NetworkRetry`, return nil/empty gracefully), `searchAndPersist` never throws. UI states verified: `ProductSearchView` (loading/prompt/empty), meal sheet (loading + deterministic fallback), `ProductScanView` (unrecognized + search), `MealScanView` (analyzing + `errorView`). Fixed last `try? modelContext.save()` → `saveOrLog("tracker", surface: true)` in `MealScanView.saveMeal`. Build green. **Next: E2.**
- **E2** `done` — Added `CustomProductStore.cleanupDuplicates(in:)` (merges rows sharing a normalized name, keeps newest, merges aliases, deletes rest) wired into `TrackerView.onAppear` for a one-time self-heal. A11y labels added (Search/Add protocol buttons; search field + result Add already labeled in C1). Build green. **Next: E3.**
- **E3** `done` — Tests + full-module smoke pass. Added `PulseLoopTests/MealEstimatorTests.swift` (6 tests: clean JSON, JSON-in-prose, emoji-symbol rejection, fallback name, no-calories nil, quick-estimate) and 2 `cleanupDuplicates` tests in `CustomProductStoreTests`. Full suite: **211 tests, 0 failures** (`TEST SUCCEEDED`). **Phase E complete — roadmap done.**

---

## Log
_(append one entry per completed iteration)_

- **A1** (iteration 1): New `CustomProductInfo` SwiftData model — the reusable catalog row for AI/web/API-discovered products. Files: `Models/LifeOSModels.swift` (model), `Persistence/ModelContainerFactory.swift` (registered in `coreModels`), `Platform/SubApps/ProtocolSubApp.swift` (added to `models`). Additive only; `BUILD SUCCEEDED`. Follow-ups: none. Next pending: **A2** (persistence + lookup façade).
- **A2** (iteration 2): Persistence + lookup façade `CustomProductStore` + search integration. Files: `Services/CustomProductStore.swift` (new), `Services/ProductSearchService.swift` (`search(query:customMatches:)` overload consulting persisted entries before network). De-dupe by normalized name+aliases; conversions via `SupplementInfo`. `BUILD SUCCEEDED`. Follow-ups: callers must fetch `customMatches` on the main actor and pass them in (handled when search UI lands in C). Next pending: **A3** (version bump + update flow).
- **A3** (iteration 3): Version bump. Files: `Platform/SubApps/ProtocolSubApp.swift` + `NutritionSubApp.swift` (`version "1.1.0"`), `PulseLoopTests/ModuleVersioningTests.swift` (+2 tests). Additive model → no migration/confirmation. `TEST SUCCEEDED` (12/12). Follow-ups: none. **Phase A done.** Next pending: **B1** (AI research pass / Perplexity tier).
- **B1** (iteration 4): AI research tier `ProductResearchService` + unified result type. Files: `Services/ProductResearchService.swift` (new), `Services/ProductSearchService.swift` (result type + `.aiResearch` tier), `Views/ProductScanView.swift` (switch case), `PulseLoopTests/ProductResearchTests.swift` (new, 6 tests). Web-grounded via OpenRouter `:online`, typed decode, graceful degrade. `TEST SUCCEEDED`. Follow-ups: none. Next pending: **B2** (persist-on-discovery).
- **B2** (iteration 5): Persist-on-discovery. Files: `Services/ProductSearchService.swift` (`searchAndPersist` + `PersistedSearch`, `import SwiftData`), `PulseLoopTests/CustomProductStoreTests.swift` (new, 3 tests). Saves AI/uncatalogued-API results as `CustomProductInfo`, de-duped, seeds future searches. `TEST SUCCEEDED`. Follow-ups: none. Next pending: **B3** (make `searchAndPersist` the single engine for add-protocol).
- **B3** (iteration 6): Add-protocol uses the unified engine. Files: `Views/TrackerView.swift` (`aiSearchIngredient` → `searchAndPersist`; autocomplete includes persisted custom entries). `BUILD SUCCEEDED`. **Phase B done.** Next pending: **C1** (search UI in Protocol).
- **C1** (iteration 7): AI-first search surface. Files: `Views/ProductSearchView.swift` (new — `ProductSearchView` + `ProductResultCard`), `Views/TrackerView.swift` (Search button + sheet + `adoptSearchResult`). `BUILD SUCCEEDED`. Next pending: **C2** (meals search + NL capture).
- **C2** (iteration 8): AI-first meal capture. Files: `Services/MealEstimator.swift` (new), `Views/TrackerView.swift` (meal sheet live estimate + `estimateMeal`/`saveNewMeal`). `BUILD SUCCEEDED`. Next pending: **C3** (label scan through unified engine).
- **C3** (iteration 9): Scan/search persistence. Files: `Views/ProductScanView.swift` (`searchOnline` + `matchProduct` fallback → `searchAndPersist`; `addToProtocol` upserts to `CustomProductStore` + `saveOrLog`). `BUILD SUCCEEDED`. **Phase C done.** Next pending: **D1** (`search_product` Coach tool).
- **D1** (iteration 10): `search_product` Coach tool. Files: `Coach/Tools/ProtocolTools.swift` (new `searchProduct` in `writeTools`). `BUILD SUCCEEDED`. Next pending: **D2** (`log_meal` Coach tool).
- **D2** (iteration 11): `log_meal` auto-estimate. Files: `Coach/Tools/DailyLifeTools.swift` (`logMeal` uses `MealEstimator.estimate` when calories omitted). `BUILD SUCCEEDED`. **Phase D done.** Next pending: **E1** (robustness/error states).
- **E1** (iteration 12): Robustness. Files: `Views/MealScanView.swift` (`saveMeal` → `saveOrLog`). Audited engine for force-unwraps (none) + UI states (covered). `BUILD SUCCEEDED`. Next pending: **E2** (a11y + catalog cleanup).
- **E2** (iteration 13): Catalog cleanup + a11y. Files: `Services/CustomProductStore.swift` (`cleanupDuplicates`), `Views/TrackerView.swift` (onAppear cleanup + a11y labels). `BUILD SUCCEEDED`. Next pending: **E3** (tests + smoke).
- **E3** (iteration 14): Tests + smoke. Files: `PulseLoopTests/MealEstimatorTests.swift` (new, 6), `PulseLoopTests/CustomProductStoreTests.swift` (+2 cleanup). Full suite **211 tests, 0 failures** (`TEST SUCCEEDED`). **ROADMAP COMPLETE.**
